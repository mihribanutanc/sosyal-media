server:
  port: 9090

  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://auth-application
          predicates:
            - Path=/dev/v1/auth/**
        - id: user-service
          uri: lb://user-application
          predicates:
            - Path=/dev/v1/user-profile/**